<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.vanguard.testops.workflow.mapper.WorkflowPublicNodeMapper">

    <resultMap id="WorkflowPublicNodeResultMap" type="io.vanguard.testops.workflow.domain.WorkflowPublicNode">
        <id column="id" property="id"/>
        <result column="project_id" property="projectId"/>
        <result column="name" property="name"/>
        <result column="description" property="description"/>
        <result column="type" property="type"/>
        <result column="category" property="category"/>
        <result column="config" property="config" typeHandler="com.baomidou.mybatisplus.extension.handlers.JacksonTypeHandler"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="create_user" property="createUser"/>
        <result column="update_user" property="updateUser"/>
        <result column="deleted_time" property="deletedTime"/>
        <result column="deleted_by" property="deletedBy"/>
    </resultMap>

    <!-- 根据项目ID查询节点列表（排除已删除） -->
    <select id="selectByProjectId" resultMap="WorkflowPublicNodeResultMap">
        SELECT * FROM workflow_public_node
        WHERE project_id = #{projectId} AND deleted_time IS NULL
        ORDER BY create_time DESC
    </select>

    <!-- 根据项目ID和分类查询节点列表（排除已删除） -->
    <select id="selectByProjectIdAndCategory" resultMap="WorkflowPublicNodeResultMap">
        SELECT * FROM workflow_public_node
        WHERE project_id = #{projectId} AND category = #{category} AND deleted_time IS NULL
        ORDER BY create_time DESC
    </select>

</mapper>

